# TTGO — Agent Instructions

The canonical agent/development guidelines for this repo live in [CLAUDE.md](CLAUDE.md).
Read that file in full before making changes. It covers project structure, build
commands (note: the Go backend requires the `sqlite_fts5` build tag), code style,
and the versioning/changelog process.
